title = "signal-forge gitleaks config"

[extend]
# Start from gitleaks' built-in ruleset (AWS/GCP/Azure keys, generic API keys,
# private keys, k8s Secret YAML shape, etc.) rather than replacing it.
useDefault = true
