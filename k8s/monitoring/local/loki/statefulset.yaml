# Local single-replica Loki. The PVC keeps logs across a pod restart, but the
# filesystem backend and in-memory ring provide no replication or HA guarantee.
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: loki
  namespace: otel-lab
spec:
  serviceName: loki
  replicas: 1
  selector:
    matchLabels:
      app: loki
  template:
    metadata:
      labels:
        app: loki
    spec:
      containers:
        - name: loki
          image: grafana/loki:3.0.0
          args:
            - "-config.file=/etc/loki/loki.yaml"
          ports:
            - containerPort: 3100
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          volumeMounts:
            - name: config
              mountPath: /etc/loki
            - name: data
              mountPath: /var/loki
      volumes:
        - name: config
          configMap:
            name: loki-config
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          # Capacity is intentionally small for a lab and has no retention policy;
          # clear/recreate it when resetting experiments rather than relying on it
          # as an operational log archive.
          requests:
            storage: 2Gi
---
apiVersion: v1
kind: Service
metadata:
  name: loki
  namespace: otel-lab
spec:
  selector:
    app: loki
  ports:
    - port: 3100
      targetPort: 3100
