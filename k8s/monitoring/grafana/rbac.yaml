# Alloy watches cluster objects to discover scrape/log targets and attach
# Kubernetes metadata to telemetry. The scope is cluster-wide because nodes and
# namespaces are cluster-scoped, but every granted verb is read-only; collection
# must never mutate workloads through this ServiceAccount.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: alloy
  namespace: otel-lab
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: alloy
rules:
  - apiGroups: [""]
    resources: ["pods", "nodes", "namespaces", "endpoints", "services"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["apps"]
    resources: ["replicasets", "deployments", "statefulsets", "daemonsets"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: alloy
subjects:
  - kind: ServiceAccount
    name: alloy
    namespace: otel-lab
roleRef:
  kind: ClusterRole
  name: alloy
  apiGroup: rbac.authorization.k8s.io
