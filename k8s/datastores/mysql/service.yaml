apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: otel-lab
spec:
  selector:
    app: mysql
  ports:
    - port: 3306
      targetPort: 3306
  clusterIP: None  # headless for StatefulSet DNS
